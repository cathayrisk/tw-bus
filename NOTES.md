# 開發紀錄與心得

一天之內從「研究 Data.Taipei 有什麼」做到上線。這份紀錄的重點不是做了什麼，
而是**哪些判斷後來被資料推翻**，以及**還沒解決的問題在哪**。

---

## 一、資料源：最重要的三個發現

**1. 台北市的公車動態是免金鑰、免註冊、10 秒更新的。**
端點在 `https://tcgbusfs.blob.core.windows.net/blobbus/`，`.gz` 但沒帶
`Content-Encoding`，要自己用 `DecompressionStream('gzip')` 解。
實測連續輪詢：`UpdateTime` 每 10 秒準時推進，1650 輛車裡每格約 58% 座標變動。
帶 `Access-Control-Allow-Origin: *`，純前端可直連。

**2. `GetBusData.RouteID` 跟其他檔不是同一個 ID 空間。**
最陰的一個坑。`GetStop` 與 `GetEstimateTime` 用主路線 `Id`；
但 `GetBusData` 的 1651 輛車只有 632 筆對得上 `Id`，**1542 筆要對 `pathAttributeId`（子路線）**。
不先反查，一半的車會掛不上路線。

**3. 台北與新北是兩套系統，但 `stopLocationId` 共用。**
路線名稱交集 0、route id 交集 0、stop id 交集 0 —— 依主管機關切分，可安全合併。
唯一共用的是 `stopLocationId`（2653 個重疊），這是把同一根實體站牌併起來的鍵。
897（新北）的「新北板橋公車站」就是 70716，跟 307（台北）去程同一個月台編號。

**資料齡會落後很多。** 同一包裡最新的車 13:39:01、最舊的 13:31:24，差 8 分鐘。
一定要用 `DataTime` 算年齡並在畫面降級，否則會畫出停在路中間的幽靈公車。

---

## 二、被資料推翻的判斷（照時間順序）

### 2.1 「動態校準路廊速度」比固定速度更差

原本用「最近一台車的距離 ÷ 官方 ETA」反推路廊速度，再推估後面幾台，
還宣稱它「會自己跟著路況走」。用 78 幀資料抓到 8 次交接事件、188 組樣本實測：

| 情境 | 動態校準 | 固定 12 km/h |
|---|---|---|
| 307 去程 2–5 分 | 87 秒 | **69 秒** |
| 307 去程 5 分以上 | 149 秒 | **133 秒** |
| 307 返程 5 分以上 | **1364 秒** | **179 秒** |
| 265經明德路 | 222 秒 | **50 秒** |

每一格都輸，還會炸出 1364 秒。原因：第一台正在靠站或停等時隱含速度趨近零，
推估遠處的車就整個被放大 —— 把局部減速當成整條路廊的狀態。已棄用。

### 2.2 路況探針的時間加權偏誤

原本每 10 秒把每台車的瞬時速度堆進格子。結果卡住的車每 10 秒投一票，
卡 5 分鐘就投 30 票、票票都是 0 km/h。改成**每格每台車一票、值為實際通過速度**
（里程差÷時間差）後：

| | 舊（瞬時堆疊） | 新（一台一票） |
|---|---|---|
| 區段速度中位數 | 7.2 km/h | **12.3 km/h** |
| 判定壅塞的區段 | 22% | **2%** |

時間加權把速度中位數壓低四成、壅塞多報十倍。

### 2.3 格子綁路線是浪費樣本

改用「地理網格（約 400m）＋行進方位 8 象限」當鍵之後：達標格 18%→33%、
每格平均車數 1.6→2.32，並觀察到 29 個格子同時含有 265 與 307 兩個家族的車。
方位取自路線線型走向，不用車輛回報的 `Azimuth`（停駛時可能是 000）。

### 2.4 「前方壅塞 N 公尺」逐列重複不是 bug

三台車距離 1695/2739/4007 m 卻都顯示 492 m。因為那是「該車到站牌之間」的累計，
而遠車路廊完整包含近車路廊，所以單調遞增、常相等。
沿 307 去程掃 4.2 km 證實：壅塞 273m、緩慢 2176m、順暢 1482m、無資料僅 292m（7%），
且壅塞幾乎全在最後 2 km（2–3km 是 0m）。**不是覆蓋率問題，是路真的長這樣。**
但這個呈現方式本身錯了 —— 塞車是路的屬性，不該印在車的列上。

---

## 三、Astra（gpt-6-astra）對抗性檢視推翻的東西

送檢的提案是「先建時段基準 → 把格速度餵進 ETA → 最後才改版面」。
判決：**不建議照這三步依序投入**。其中四項打的是推理本身：

**3.1 「常態慢是噪音」是目標錯置。**
> 常態慢仍可能是改走捷運的充分理由。改成純相對偏離後，最穩定、最有決策意義的慢路段可能從畫面消失。

使用者要決定的是「我要等多久」，不是「今天比平常糟嗎」。
另外：中位數 12 km/h 只代表約半數**觀測**落在附近或以下，
推不出「一半**路段**永遠緩慢」—— 路段長度與觀測權重不同。

**3.2 逐格速度積分有數學錯誤。**
> 各格中位耗時相加通常不等於整程耗時的中位數。距離除以速度對低速格極敏感，零速無法計算。

`1/中位數 ≠ 中位數的倒數`。修正方向：直接估每格**通行時間**而非速度，
保留尚未駛出格子的車（否則漏掉卡最久的），對零速／缺格／過舊明訂回退上限。

**3.3 驗證用錯了真值。**
> 188 組交接樣本未交代是否以實際到站為真值。

我的「真值」是交接後**官方給的 ETA**，那本身也是預測。等於拿估計對估計，
所以原本標的「±1–2 分」撐不起來 —— 已降級為單純標「推估」。
正確做法：以**實際到站**為共同終點、按日期分塊留出、記錄高分位誤差與區間實際涵蓋率。

**3.4 七天分桶撐不起時段基準。**
每個星期別只有約一次日期層級的觀測機會，而且只在開頁時收集。
修正方向：先只記錄覆蓋情況；要做就存可跨多週的摘要，起步採工作日／假日與較寬時段，
設獨立日期與車次的最低要求，不足時逐級合併並顯示資料不足。

**3.5 順序有迴避嫌疑。**
> 存在用可量化的技術工作延後設計取捨的跡象。

建議先做最便宜的：可切換的呈現對照（現版 vs 路況收合版），實際用幾天再決定。

**3.6 機會成本。**
日常 10 條路線有 5 條（57、577、657、897）完全缺失，這是已知的使用障礙。
應先估「每週實際使用次數 × 接入工時」，再跟模型精修比較。

---

## 四、這一版做了什麼（依 Astra 建議）

- **正名**：「壅塞」→「低速／停滯」。量到的是公車跑多慢，其中混著紅燈，名稱要跟量測一致。
  （靠站造成的停滯已排除：速度 0 的觀測離站牌中位 80m、有速度的 83m，分布一致；紅燈無法分離。）
- **每列只講這台車自己的狀態**：`正在低速路段 6 km/h`。拿掉會重複的累計長度。
- **路況只講一次**：看板上方一條摘要，固定看**站牌前 2 km**（不隨車的位置縮放），
  覆蓋率低於 40% 直接顯示「路況樣本不足（已量到 N%）」，不假裝知道。
- **加了「路況」開關**：可整組關掉，就是 Astra 要的對照組。
- **不確定性標示降級**：「推估 ±1–2 分」→「推估」。

---

## 五、還沒做的，依 Astra 的排序

1. **版面對照實驗**（最便宜）。路況開關已經做好，實際用幾天，記錄判讀時間與後悔次數。
2. **評估新北接入**。先估 57／577／657／897 每週實際使用次數與接入工時。
   走 TDX 可拿到 ETA＋車輛 GPS＋線型，功能與 307 同級，但要 OAuth2 金鑰＋小後端
   （Cloudflare Workers 免費層足夠）；走新北開放平台只有 ETA、沒 GPS、沒線型、沒 CORS。
3. **兩個模型留在背景驗證**，不要直接上線：
   - 逐格通行時間模型 vs 現行固定 11.5 km/h ＋ 60 秒，以實際到站為真值、按日期留出。
   - 歷史基準：先只記錄桶占用率、獨立日期數、資料年齡，再決定分桶細度。

### 還缺的量測（Astra 列的）
- 實際開頁時長，以及每桶的獨立車數、獨立日期數、空桶率、資料年齡。
- 188 組交接樣本的真值定義、誤差定義、切分方式。
- 逐格觀測的完成／未完成穿越紀錄。
- 10 條日常路線的實際使用頻率與後端維護工時。

---

## 六、幾個工程上的小結

- **localStorage 有同步寫入與容量限制**，長期軌跡累積要改 IndexedDB（仍是純前端）。
- **推估與官方要分開標示到底。** 這個 app 最有價值的地方不是數字多準，
  而是你隨時知道哪個數字是資料源給的、哪個是我算的。
- **覆蓋率不足時顯示「不知道」比顯示綠色好。** 沒資料不等於順暢。
- **不要用另一個預測當真值。** 這是這次最貴的一課，而且我自己沒抓到。
